{
  "name": "abyssal-wake",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check:worker": "wrangler deploy --dry-run --outdir=.wrangler-dist",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}

name: Deploy Deep Sea Duo to GitHub Pages

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: deep-sea-duo-pages
  cancel-in-progress: true

# GitHub App connections cannot perform the repository-admin step required
# for first-time Pages enablement. Netlify is the active public deployment.
# Keep this workflow manual-only as a future fallback if Pages is enabled later.
jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload game files
        uses: actions/upload-pages-artifact@v4
        with:
          path: deep-sea-duo

      - name: Deploy GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

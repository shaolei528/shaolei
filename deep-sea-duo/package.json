{
  "name": "deep-sea-duo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "find src -name '*.js' -print0 | xargs -0 -n1 node --check && node --check sw.js && node --check netlify/functions/room.mts"
  },
  "dependencies": {
    "@netlify/blobs": "latest",
    "@netlify/functions": "latest"
  }
}
